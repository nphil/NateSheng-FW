GGFW Clean 5.5.0 Stage10A Hotfix4 - Messenger UI consistency

Changes:
- Messenger home menu reverted to the same SmallNormal + inverted-selector style as the sub-lists.
- Removed the arrow-style selector from the Messenger home screen.
- Fixed inverted row text drift: selected list text is now drawn left-aligned inside the inverse capsule instead of being centered/outside it.
- Kept the improved envelope icon, unread clear behavior, and independent MSG BEEP behavior from Stage10A hotfix1.
- No RF/FSK/storage logic changes.

Build with VS Code:
- Cmd+Shift+B -> GGFW: Build Fusion (Docker)
- If permission error: Cmd+Shift+B -> GGFW: Make build script executable
